# TODO: Add Total Entry Counts and Custom Date Range Filters

## Tasks for Leads Page (src/app/Leads/page.js)
- [ ] Add total leads count display in the header (e.g., "Total Leads: {filteredLeads.length}")
- [ ] Add custom date range inputs (start date and end date) in the filter section
- [ ] Update filtering logic to handle custom date ranges
- [ ] Ensure count updates with month selections, filters, and custom ranges

## Tasks for Assignments Page (src/app/Components/AssignmentPage.js)
- [ ] Add total assignments count display in the header (e.g., "Total Assignments: {filteredAssignments.length}")
- [ ] Add custom date range inputs (start date and end date) in the filter section
- [ ] Update filtering logic to handle custom date ranges
- [ ] Ensure count updates with month selections, filters, and custom ranges

## Testing
- [ ] Test Leads page: Verify counts update with calendar month changes, radio filters, and custom date ranges
- [ ] Test Assignments page: Verify counts update with calendar month changes, radio filters, and custom date ranges
- [ ] Ensure date range filtering works correctly for both pages
